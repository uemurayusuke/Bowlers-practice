<!DOCTYPE html>
<html>
  <head>
    <title>Bowlers</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>


    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.6.2/css/bootstrap.min.css">
    <!--→これを適用させると画面がおかしくなる-->
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://kit.fontawesome.com/1043f0fa51.js" crossorigin="anonymous"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.6.7/js/bootstrap.min.js"></script>
  </head>


<% if user_signed_in? %>
<div><%= render 'layouts/header' %></div>
<div>
<% flash.each do |message_type, message| %>
      <div class="flash-message <%= message_type %>"><%= message %></div>
<% end %>
</div>



<div class="form-row mt-3">
 <div class="col"></div>
 <div class="col-4"><%= render 'public/searches/form' %></div>
</div>

<div class="form-row mt-3">
  <div class="col"></div>
 <div class="col-4"><%= render "public/searches/tag_form" %></div>
</div>
<% end %>


<!--元々の記述-->
  <body>
    <%= yield %>
  </body>

 <%= render 'layouts/footer' %>

</html>
